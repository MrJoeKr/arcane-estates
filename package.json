{
  "name": "arcane-estates",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=@arcane-estates/web",
    "dev:server": "turbo run dev --filter=@arcane-estates/server",
    "build": "turbo run build",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  }
}
